class AppVectors{
  static const String basePath='assets/vectors/';
  static const String format=".svg";

  static const logo="${basePath}spotify_logo${format}";
  static const moon="${basePath}moon${format}";
  static const sun="${basePath}sun${format}";
  static const bottom_pattern="${basePath}bottom_pattern${format}";
  static const top_pattern="${basePath}top_pattern${format}";
  static const home_top_card="${basePath}home_top_card${format}";

}